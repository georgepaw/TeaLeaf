#ifndef NO_ECC_CUH
#define NO_ECC_CUH

#define CHECK_CRC32C(cols, vals, a_col_index, a_non_zeros, row_begin, jj, kk, fail_function)

#define CHECK_ECC(col, val, a_col_index, a_non_zeros, idx, fail_function)

#define MASK_INDEX(index) index

#endif //NO_ECC_CUH